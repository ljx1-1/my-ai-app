# ===== PDF 文档问答系统 - 依赖清单 =====
# 生成依据：扫描 streamlit_app.py / st_app.py / simple_ai.py 中的 import

# Streamlit 前端框架（streamlit_app, st_app）
streamlit==1.32.2

# 调用 DeepSeek API（streamlit_app, st_app, simple_ai）
openai==1.14.3

# PDF 文本提取（streamlit_app）
pdfplumber==0.10.3

# 向量数据库，存储文档片段（streamlit_app）
chromadb==0.4.24

# Chroma 默认嵌入模型 all-MiniLM-L6-v2 的运行依赖
sentence-transformers==2.5.1
